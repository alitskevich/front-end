export class NpmRepo {

  constructor(db, folder, index, config) {

    this.db = db;
    this.index = index;
    this.folder = folder;
    this.config = config;
  }

  publish({ path }) {

  }

  install(data) {

  }
}
