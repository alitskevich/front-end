import assert from 'assert';
import { New, ObjectConstructor } from './native';

describe('Curry', function () {

  it('ObjectConstructor', () => {

    // const obj = New(ObjectConstructor);
    //
    // assert.equal(obj, 1);
  });
});
